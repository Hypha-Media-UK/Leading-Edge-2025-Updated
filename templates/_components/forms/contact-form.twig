{# Contact Form Component - Now using Formie #}
<div class="form-header">
    <h2>{{ entry.cb_contactForm.blockTitle }}</h2>
    <p>{{ entry.cb_contactForm.formIntro }}</p>
</div>

{{ craft.formie.renderForm('generalContact', {
    themeConfig: {
        form: {
            attributes: {
                class: 'contact-form',
            },
        },
        submitButton: {
            attributes: {
                class: 'btn primary',
            },
        },
    },
}) }}
